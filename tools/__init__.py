"""Dev-only tooling that runs outside the engine (log analysis, etc.).

Nothing under here is loaded by the bot process (`__main__.py`) -- it is
for offline inspection of `.mmgl` replay logs after a match.
"""
